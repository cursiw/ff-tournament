version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - ADMIN_API_KEY=change_me_to_a_strong_key
      - NODE_ENV=production
